const menu=document.querySelector('.menu');const nav=document.querySelector('nav');menu.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',String(open));});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menu.setAttribute('aria-expanded','false');}));
document.querySelector('#contact-form').addEventListener('submit',event=>{event.preventDefault();const form=event.currentTarget;if(!form.reportValidity())return;const data=new FormData(form);const nome=String(data.get('nome')).trim();const telefone=String(data.get('telefone')).trim();const mensagem=String(data.get('mensagem')).trim();if(!nome||!mensagem){alert('Preencha seu nome e a mensagem.');return;}const text='Olá! Encontrei a Auto Mecânica Pai e Filho pelo site e gostaria de solicitar um atendimento.'+'\n\nNome: '+nome+'\nTelefone: '+telefone+'\nMensagem: '+mensagem;window.location.assign('https://wa.me/5534991495410?text='+encodeURIComponent(text));});